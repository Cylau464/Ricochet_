namespace Enums
{
    public enum GameMode { Classic }
    public enum SkinType { Hero, Shield }
    public enum SkinRarity { Common, Rare, Mythic, Epic}
    public enum AdType { Interstitial, Rewarded, Banner }
}